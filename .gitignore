# Local preview server (dev only — never deploy)
__serve.bat
__serve.ps1

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# Dependencies (if ever added)
node_modules/

# Design canvas build output (regenerated from .design/*.dc.html)
.design/avoxan-rebuild.html
